include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - lib/src/generated/**
  errors:
    # The generated bindings use C-style names; the typed API wraps them.
    non_constant_identifier_names: ignore
