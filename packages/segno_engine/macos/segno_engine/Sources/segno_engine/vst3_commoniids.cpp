// Forwarder TU (SPM macOS build) — compiles the vendored VST3 GUI IID
// definitions (IPlugView / IPlugFrame / IPlugViewContentScaleSupport) needed to
// queryInterface the editor view interfaces (part 6). Disjoint from the base
// IIDs in coreiids.cpp.
#include "../../../../third_party/vst3sdk/public.sdk/source/common/commoniids.cpp"
