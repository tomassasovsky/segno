// Forwarder TU (SPM macOS build) — see plugin_probe.cpp for the pattern.
// Pulls in the shared plugin-scan C ABI + scan thread from <plugin>/src/host.
#include "../../../../src/host/plugin_scan.cpp"
