// Forwarder TU (CocoaPods macOS build) — the vendored VST3 GUI IID definitions
// (IPlugView / IPlugFrame) needed for the editor view interfaces (part 6).
#include "../../third_party/vst3sdk/public.sdk/source/common/commoniids.cpp"
