name: bluetooth_client
description: >-
  Data client for Segno appliance Bluetooth (`segno-bt-ctl`). Pure Dart —
  Process.run + JSON parse only; no Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  equatable: ^2.1.0
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.2.0
