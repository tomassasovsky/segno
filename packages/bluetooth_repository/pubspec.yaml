name: bluetooth_repository
description: >-
  Repository for Segno appliance Bluetooth — thin orchestration over
  bluetooth_client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  bluetooth_client:
    path: ../bluetooth_client

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.2.0
