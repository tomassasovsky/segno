# Point Dropbear at host keys on /data (segno-ssh-persist). Overrides the
# unit's default DROPBEAR_RSAKEY_DIR=/etc/dropbear; stock /etc/default/dropbear
# still supplies DROPBEAR_EXTRA_ARGS.
[Service]
Environment=DROPBEAR_RSAKEY_DIR=/data/dropbear
