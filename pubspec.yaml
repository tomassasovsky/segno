name: segno
description: Segno — a cross-platform desktop loopstation with a native low-latency audio engine
version: 1.0.0+1
publish_to: none
license: GPL-3.0-or-later

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  bloc: ^9.2.1
  bluetooth_repository:
    path: packages/bluetooth_repository
  brightness_client:
    path: packages/brightness_client
  console_facts_client:
    path: packages/console_facts_client
  controller_repository:
    path: packages/controller_repository
  daw_export:
    path: packages/daw_export
  desktop_multi_window: ^0.3.0
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.3
  looper_repository:
    path: packages/looper_repository
  lucide_icons_flutter: ^3.1.15
  midi_device_repository:
    path: packages/midi_device_repository
  path_provider: ^2.1.6
  pedal_repository:
    path: packages/pedal_repository
  performance_repository:
    path: packages/performance_repository
  routing_graph:
    path: packages/routing_graph
  # Multi-monitor detection: the output-waveform window goes full-bleed on a
  # secondary display when one is present. Already resolved (transitive via
  # window_manager); declared direct so we can import its display API.
  screen_retriever: ^0.2.0
  session_repository:
    path: packages/session_repository
  settings_repository:
    path: packages/settings_repository
  toastification: ^3.2.0
  update_repository:
    path: packages/update_repository
  url_launcher: ^6.3.0
  wifi_repository:
    path: packages/wifi_repository
  window_manager: ^0.5.2

dev_dependencies:
  bloc_lint: ^0.4.1
  bloc_test: ^10.0.0
  bloc_tools: ^0.1.0-dev.24
  # Data packages used directly by the test suite only (fakes + integration
  # tests import them); production code reaches them transitively via the
  # repositories, so they are dev-only here.
  bluetooth_client:
    path: packages/bluetooth_client
  # The control-sequence fuzzer (test/fuzz/) runs under controlled time; the
  # package resolves via flutter_test anyway, declared explicitly for
  # depend_on_referenced_packages.
  fake_async: ^1.3.1
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  midi_client:
    path: packages/midi_client
  mocktail: ^1.0.5
  segno_engine:
    path: packages/segno_engine
  very_good_analysis: ^10.3.0
  wifi_client:
    path: packages/wifi_client

flutter:
  generate: true
  uses-material-design: true
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
    - family: JetBrains Mono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Regular.ttf
        - asset: assets/fonts/JetBrainsMono-Medium.ttf
          weight: 500
        - asset: assets/fonts/JetBrainsMono-SemiBold.ttf
          weight: 600
