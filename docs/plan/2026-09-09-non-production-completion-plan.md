# Non-production completion

Issue 919. The owner requested every remaining design item and a recorded
walkthrough, excluding production implementation. This authorizes the local
prototype work below; it does not authorize publishing or merging.

## Scope and observable results

1. Use one selected-track render contract for Bounce and Save audio. Show the
   resulting duration and a deliberate shared-mix-FX choice. Retain each
   source's individual processing and level; create a neutral destination.
   Performance recording includes the routed mix and output effects, with an
   explicit choice to follow final output volume and mute. Freeze that choice
   for the take. Demonstrate sound and saved-signal consequences.
2. Connect the Stop, Clear, Mute, bypass and explicit sound-cut policies to an
   understandable executable example. Preserve printed Pre audio and existing
   Pre/Post controls. Demonstrate tails audibly, with illustrative Web Audio
   clearly distinguished from Segno's unimplemented audio engine.
3. Finish timing boundaries using a musical cycle independent of audible
   direction and speed; preserve sparse recordings, recover clock loss,
   choose a successor when clearing the primary, and allow correcting first
   take inference without changing captured audio duration.
4. Recover verified FX metadata and factory audio from supplied or official
   sources. Never invent missing control domains or claim substitute audio is
   the factory collection. Record any exact missing source as an external
   blocker while completing all independent work.
5. Add optional touch lock, double-press Solo, explicit expanded MIDI source
   protocols, and direct USB performance recording. USB takes keep their
   original drive identity; disconnect, slow writes and low space preserve
   saved parts. Internal recording and later export remain available.

## Ownership and validation

The coordinator owns recording, rendering, the main HTML adapter, integration,
Pen and the recorded walkthrough. Native agents own timing, optional controls,
and FX/reference evidence respectively. Shared-file adapter changes are sent to
the coordinator instead of simultaneous HTML edits.

Run meaningful deterministic behavior tests, Chrome and Firefox journeys,
save/reload and failure checks. Complete independent architecture, test,
simplicity, conventions and readiness reviews, then verify matching editable
Pen screens. Capture actual interactions as a chaptered video; do not substitute
still images or imply browser fixtures are real device capture.

No native audio, hardware drivers, actual storage transactions or Linux app
implementation are included. Production and appliance measurements remain open.
