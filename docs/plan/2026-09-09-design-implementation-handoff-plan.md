# Final design comparison and implementation handoff

The owner authorized completing the currently actionable non-production work,
then preparing the full accepted context and an efficient Claude Fable 5.1 prompt
for a later agent to implement the redesign throughout the existing app.
This turn does not launch that production implementation or publish repository work.

1. Reconcile accepted behavior, actual app entry points and unresolved source gates.
2. Compare the 19 older completion Pen screens with current browser geometry;
   repair mismatches, synchronize the primary-track crown in Track/Wave/Mixer
   and the selected-track display, and remove section overlap.
3. Save and verify Pen. Keep the accepted instrument section intact.
4. Produce a compact entry prompt, accepted behavior contract, implementation
   map, evidence/source-gate inventory and transfer manifest. Research the
   official Fable 5.1 guide; cite it and distinguish prompt guidance from harness
   configuration. The prompt must preserve scope through long runs and compaction.
5. Verify links, manifests, source fidelity, appropriate prototype regressions,
   and independent handoff reviews. Reconcile roadmap/PROGRESS so old proposal
   prose cannot silently reopen approved flows.

```success-criteria
GOAL: Deliver a complete, reviewable design handoff for the existing Segno app.
SUCCESS CRITERIA:
- Accepted workflows are captured without contradictory superseded decisions | verify: independent user-flow review
- Existing Flutter/native seams and first vertical implementation slice are concrete | verify: codebase review and source paths
- Older completion screens and primary crown match current prototype and are saved in Pen | verify: native geometry, screenshots and on-disk save
- Entry prompt uses official Fable 5.1 guidance and links a bounded context pack | verify: source check and handoff review
- True external-source and hardware gates remain explicit, while locally actionable items are finished | verify: delivery checklist and reference inventory
NON-GOALS:
- Production app implementation, hardware validation, or publishing this dirty checkout
- Inventing missing Looper X parameter definitions or claiming prototype audio is production
```
